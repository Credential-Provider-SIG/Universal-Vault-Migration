{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "OpenBox",
    "description": "Open Box to initialise the vault exchange. The OpenBox is generated by the vault receiver and transmitted to the Vault provider.",
    "type": "object",
    "properties": {
        "publicKey": {
            "type": "string",
            "format": "base64",
            "description": "Public Key of the Vault receiver used to share the vault content symmetric key with the vault source."
        }
    },
    "required": [
        "publicKey"
    ]
}
